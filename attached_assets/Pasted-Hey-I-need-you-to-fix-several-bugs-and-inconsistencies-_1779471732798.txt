Hey, I need you to fix several bugs and inconsistencies across the will detail screens. Please address all of them:
Bug #1 — "Invalid Date – Invalid Date" in the Duration progress ring
The Duration will's progress ring is rendering "Invalid Date – Invalid Date" below it. This is a date parsing failure — the start and end date values are not being passed correctly into the ring component and are arriving as null or undefined. Trace where the will's startDate and endDate are being passed into the progress ring component and ensure they are properly parsed and formatted before rendering. Add a null/undefined guard so that if dates are missing, the label simply doesn't render rather than showing "Invalid Date."
Bug #2 — Calendar dot not turning green after checking in (Solo Duration + Team Duration screens)
On both the Solo Duration and Team Duration screens, when the user taps "Did it" or checks in for today, the calendar dot for today stays as the purple "today" outline instead of flipping to a green filled dot. The check-in action is not writing back to the calendar's day state, or the component is not re-rendering after the update. Fix this so that immediately after a successful check-in, today's dot updates from the "today" state to the "done" (green filled) state in real time without requiring a page refresh.
Design Issue #3 — Remove the date range label from inside the progress ring card (Solo Duration + Team Duration screens)
Once Bug #1 is fixed, remove the date range label that sits below the ring entirely. It should not live inside the progress card. The timeline information (start and end dates) will be handled by the condensed status line at the top of the screen — it does not need to appear again inside the progress section.
Inconsistency #4 — Kill all dropdowns on progress sections across all will types
Currently Solo Duration has the progress section hidden behind a dropdown chevron, while Team Duration shows it fully expanded. These must match. The rule going forward is: the progress section is always fully visible on every will type — Recurring, Duration, and Event — in both Solo and Team formats. Remove every chevron, toggle, and collapsible behavior from the progress section across the entire app. No dropdowns anywhere in progress.
Inconsistency #5 — "Did it" button color mismatch (Solo Recurring vs Team Recurring screens)
The "Did it" button on the Solo Recurring screen uses a darker forest green, while the Team Recurring screen uses a brighter green. These must be identical. Define one single green color token for the "Did it" button and apply it consistently across every screen in the app — Solo Recurring, Team Recurring, Solo Duration, Team Duration, and all other screens where this button appears. One green, defined once, used everywhere.
Inconsistency #6 — "HOW DID IT GO?" label missing on Team Recurring screen
The "HOW DID IT GO?" label appears correctly above the Did it / Did not buttons on the Solo Recurring screen but is missing on the Team Recurring screen. Add it to the Team Recurring screen so it appears consistently above the check-in buttons on all Recurring will screens — both Solo and Team.
Inconsistency #7 — Event type uses single button instead of two (Solo Event + Team Event screens)
On both the Solo Event and Team Event screens, the check-in CTA is currently a single full-width button labeled "Did you complete this?" This needs to be replaced with two side-by-side buttons matching the pattern used on Recurring: a green "Done" button on the left and a red-bordered "Not yet" button on the right. Remove the single button entirely and replace it with the two-button layout on both the Solo Event and Team Event screens.
Inconsistency #8 — Duplicate "YOUR PROGRESS" label on Team Recurring screen
On the Team Recurring screen, the section label "YOUR PROGRESS" appears twice — once above the Did it / Did not buttons and again as a header inside the progress card below. Remove one of them. Keep the label inside the progress card and remove the one sitting above the buttons.