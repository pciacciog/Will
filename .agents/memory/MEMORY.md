- [Neon production DB schema drift](neon-schema-drift.md) — new schema columns must be manually migrated to the Neon production DB; `db:push` only updates the Replit dev PostgreSQL.
- [Capacitor HTTP bridge stall on iOS](capacitor-http-bridge-stall.md) — CapacitorHttp.enabled=true routes all fetch() through native bridge; concurrent bridge calls stall ~60s; fix: localStorage in SessionPersistence + placeholderData in queries.
- [ESM bundle require() trap](esm-bundle-require-trap.md) — `require()` is undefined in the esbuild ESM `dist/index.js`; deployed deep-link routes 500 while `/` works. Use import, and test a non-root route on the real deployment.
- [Identifier display rules](identifier-display-rules.md) — social=@username, accountability=firstName, email NEVER in social APIs (watch proof-feed leak); username enforced app-layer (shared/username.ts + 23505→409), intercept before paywall.
- [WILL subscriptions (Stripe web + Apple IAP iOS) & build](will-stripe-and-build.md) — frontend served from prebuilt dist/public (client changes need `npm run build`); dual-source access (Stripe OR RevenueCat) w/ per-source outage rules; RevenueCat via connectors-sdk proxy, Capacitor not Expo; pin purchases-capacitor v11 for Capacitor 7.
- [WILL public-vs-solo classification & joinability](will-public-classification.md) — `visibility='open'` is the default for EVERY will so it must NOT mean public; public test = `kind==='public' || visibility==='public' || parentWillId`. Public wills MUST be joinable: the whole join stack gates on `kind==='public'`; legacy `solo+public` rows (view-only, no Join) were migrated to `kind='public'`.
